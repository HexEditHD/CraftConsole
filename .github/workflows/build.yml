name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Build
        run: dotnet build CraftConsole.slnx -c Release --no-incremental

      - name: Test
        run: dotnet test tests/CraftConsole.Tests/CraftConsole.Tests.csproj -c Release --no-build
